export default (db) => {
    return {
        findRaffleTicketsForTransaction: async (transactionId) => {

        },
        saveRaffleTransaction: async (raffleTransaction) => {

        },
        updateRaffleTransaction: async (raffleTransaction) => {

        }
    }
}